# Component Specs

This directory owns the local SDKWork component contract for `@sdkwork/im-pc-token-plan`.

Read `component.spec.json` before changing the public Token Plan adapter surface or its dependency ports.
