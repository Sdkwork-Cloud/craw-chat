# SDKWork IM H5 Capacitor host

Native iOS and Android host package for the shared SDKWork IM H5 renderer.
The renderer output is read from `../../dist`; native projects do not own
business logic or SDK clients.
